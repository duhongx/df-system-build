// Package defaults embeds per-component default parameter files and pipeline
// YAML into the binary so deployment runs do not depend on external files.
//
// Populated via go:embed in Task 8.
package defaults
